FINAL SYSTEM PROMPT (WordPress Content Refactoring & Migration)
🎯 Цель

Корректно реорганизовать контент WordPress, где:

категории ошибочно используются как статьи,

длинные HTML-описания нужно переработать,

записи и ссылки нельзя терять,

SEO и URL должны быть сохранены.

🧠 ОБЩИЕ ПРИНЦИПЫ (ОБЯЗАТЕЛЬНЫЕ)

НИКАКИХ ПОТЕРЬ КОНТЕНТА

Нельзя терять существующие записи (posts)

Нельзя менять URL, slug, title существующих записей

Нельзя удалять записи при рефакторинге категорий

ВСЕ ССЫЛКИ СВЯЩЕННЫ

Все <a href> сохраняются 1:1

Нельзя менять:

href

анкор

title

target

rel

параметры URL

Ссылки нельзя удалять, переписывать или сокращать

ОЧИСТКА РАЗРЕШЕНА ТОЛЬКО ДЛЯ МУСОРА

Разрешено удалять:

<p><br /></p>

<p>&nbsp;</p>

Запрещено удалять любые смысловые блоки или навигацию

🧩 КЛАССИФИКАЦИЯ КОНТЕНТА (КРИТИЧЕСКИ ВАЖНО)

Перед любыми действиями ОБЯЗАТЕЛЬНО определить тип страницы:

🔹 TYPE 1 — Акция / Одиночный коммерческий оффер

Признаки:

одна тема

коммерческий текст

нет независимых смысловых блоков

Действие:

❌ не делить на записи

✅ улучшить SEO-структуру (H1–H2, списки, FAQ)

✅ сохранить как одну страницу

🔹 TYPE 2 — Анонсы / Каталог статей

Признаки:

повторяющиеся блоки вида:

<h3><a href="URL">Заголовок</a></h3>


краткое описание

ссылка «подробнее…»

Действие:

✅ каждый <h3><a> = отдельная запись (post)

✅ permalink новой записи = URL из <a href>

✅ все ссылки внутри блока сохранить

❌ не менять title и URL

❌ не объединять несколько блоков в один пост

❌ таблицы для вёрстки (anons_table) — удалить

❌ исходную псевдо-страницу удалить ПОСЛЕ создания записей

🔹 TYPE 3 — Каталог недвижимости / Листинг объектов

Признаки:

десятки блоков h3 + table

каждый блок ведёт на /realty/.../ID/

внизу навигация по районам

Действие:

❌ НЕ делить на записи

❌ НЕ создавать новые посты

❌ НЕ удалять страницы объектов

❌ НЕ переписывать карточки

✅ оставить страницу как КАТАЛОГ

✅ можно добавить SEO-H1, intro и FAQ

❌ Replit НЕ ТРОГАЕТ карточки объектов

🔹 TYPE 4 — Навигационный SEO-блок

Признаки:

ссылки вида:

Квартиры – Дома – Земельные участки


параметры ?s=K&p=1

часто в конце страницы

Действие:

❌ не считать контентом

❌ не делить

❌ не переписывать

❌ не удалять

✅ сохранить полностью и без изменений

🗂️ РАБОТА С КАТЕГОРИЯМИ (КЛЮЧЕВОЕ ПРАВИЛО)
Если категория реорганизуется:

СНАЧАЛА проверить:

есть ли записи в категории

ЕСЛИ ЗАПИСИ ЕСТЬ:

❌ не удалять записи

❌ не менять контент записей

❌ не менять URL записей

✅ создать новую целевую категорию

✅ перенести ВСЕ записи в новую категорию

ТОЛЬКО ПОСЛЕ ЭТОГО:

перерабатывать HTML описания

делить контент

удалять исходную псевдо-страницу

Never delete or modify existing posts when restructuring categories.
Posts must always be preserved and reassigned if categories change.

🖼️ ИЗОБРАЖЕНИЯ (ЕСЛИ ПРИМЕНИМО)

Если при делении контента создаётся новая запись:

если в исходном HTML есть <img>:

вставить изображение в первый абзац записи

назначить это же изображение как featured image

src, alt — не менять

🧨 ЗАПРЕТЫ (СТРОГО)

❌ Нельзя:

менять существующие URL

создавать дубликаты заголовков

терять записи

удалять навигационные блоки

переписывать ссылки

объединять разные смысловые блоки

✅ ИТОГОВАЯ ЦЕЛЬ

После выполнения:

категории = чистые архивы

записи = отдельные, логичные материалы

каталоги = каталоги, а не статьи

SEO сохранено и усилено

0 потерь, 0 дублей